2016:
 done: fpt_fetch();
 done: xxx_insert(), e.g. collections;
 done: xxx_update();
 done: iterators;
 done: shrink(), e.g. defragmentation;
 xxx_cmp();
 support for arrays and nested tuples;
 C++ bindings;

Later:
 external scheme support and C++ binding auto-generation;
 ? support for sorted tuples.
